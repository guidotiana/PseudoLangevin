# main inputs

## model
from ../model_trained/run3/model_trained.pt		#path to the .pt file containing the starting weights (if None, gaussian initialization of the weights)
d 64		#embedding size
H 1			#number of heads 
m 256		#number of neurons of FFNN
L 1			#number of encoder block
n 86		#length of sequence


## dataset
filename ../sequences/SequencesMasked.txt		#file containing the data
n_data 1000				#total number of data points
validation_rate 0.1		#fraction of validation data compared to the whole dataset


## cost function
cost CE					#cost function employed during the sampling
zeta 0.5				#additive parameters for the cost function


## metric function
metric accuracy_CE		#metric function employed during the sampling


## generator
generator_seed 42		#torch generator seed


## sampler
stime_list  [1.0e+2]	#list of simulated times (moves=stime/dt)
T_list      [1.0e-4]	#list of sampled temperatures
Tratio_list [1.0e-2]	#list of ratios between the sampled temperatures and the mini-batch effective temperatures
dt_list     [1.0e-2]	#list of single-integration time steps
m1_list     [0.2]		#list of mobilities to compute the correct mass of the system (where mobility^2+inertia^2=1, with 0<mobility<1)

lamda 1.0e-6				#regularization term coefficient
mbs 64						#mini-batch size employed for the computation of the gradients during the dynamics
max_len_grads 20			#maximum size of the gradients lists (must be an even positive number)
max_len_vars 100			#maximum size of the variances list (>=min_len_vars)
min_len_vars 10				#minimum size of the variances list (necessary to update the estimate of the mini-batch noise variance)
min_var_weight 1.0e-4		#minimum weight associated to the first element of the list of variances when computing the weighted average
threshold_full_update 0.01	#request on the data lists stationarity to start and update the integration parameters (TODO)
max_extractions 5000		#maximum number of mini-batch extractions during the starting estimate of var
extractions 200				#mini-batch extractions added to improve the first estimate of var
threshold_est 0.05			#threshold for convergence of the first estimate of var
